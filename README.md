# lora-X-Interpretability
We are investigating what LoRA fine-tuning does to a language model's internal feature representations using Sparse Autoencoders, specifically whether adapters reuse existing base model features or create entirely new ones.
